$('ul.usings').prepend("<%= escape_javascript(render(:partial => 'using', :locals => {:using => @using})) %>");
$('#new_using')[0].reset();
$('.token-input-token').remove();
$('.token-input-input-token input').show();