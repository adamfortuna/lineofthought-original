<% title "Edit Sites using #{@tool.name}" %>

<section id="content" class="main full">
  <%= render 'tools/header' %>
  
  <section class="tab">
    <%= render "form" %>
  </section>
</section>

<% cache(["sidebar", @tool]) do %>
  <%= render 'tools/show_sidebar' %>
<% end %>