<aside>
  <h3>Recent Bookmark</h3>
  <ul class="lst">
    <% Bookmark.recent.limit(5).each do |bookmark| %>
      <li><%= link_to truncate(bookmark.title, :length => 40),  bookmark %></li>
    <% end %>
  </ul>
</aside>