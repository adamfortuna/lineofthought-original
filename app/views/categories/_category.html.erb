<li class="category">
  <h4 class="title"><%= link_to category.name, category_path(category) %></strong>
  <p><%= category.description %></p>
  <p>Tools Count: <%= category.tools_count %></p>
  <p>Example Tools: <%= raw tools_links(category.tools.popular(2)) %>
</li>